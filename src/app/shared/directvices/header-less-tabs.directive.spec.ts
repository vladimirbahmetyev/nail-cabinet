import { HeaderLessTabsDirective } from './header-less-tabs.directive';

describe('HeaderLessTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderLessTabsDirective();
    expect(directive).toBeTruthy();
  });
});
